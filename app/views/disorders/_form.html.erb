<%= form_with(model: disorder, local: true) do |form| %>
  <% if disorder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(disorder.errors.count, "error") %> prohibited this disorder from being saved:</h2>

      <ul>
      <% disorder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :disorder_id %>
    <%= form.number_field :disorder_id, id: :disorder_disorder_id %>
  </div>

  <div class="field">
    <%= form.label :disorder_name %>
    <%= form.text_field :disorder_name, id: :disorder_disorder_name %>
  </div>

  <div class="field">
    <%= form.label :discription %>
    <%= form.text_area :discription, id: :disorder_discription %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
