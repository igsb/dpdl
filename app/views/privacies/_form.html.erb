<%= form_with(model: privacy, local: true) do |form| %>
  <% if privacy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(privacy.errors.count, "error") %> prohibited this privacy from being saved:</h2>

      <ul>
      <% privacy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
